import { Image,Text } from 'UnityEngine.UI';
import { ZepetoScriptBehaviour } from 'ZEPETO.Script'

export default class QuestIngre extends ZepetoScriptBehaviour {

    // 재료 이름
    public ingredientName : Text;
    // 재료 이미지
    public ingredientImage : Image;

    Start() {    
        // 이름에 현재 음식이름 넣어주기
        

    }

}